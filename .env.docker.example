# Copy to .env.docker and fill passwords
# cp .env.docker.example .env.docker

HTTP_PORT=80

# Frontend build-time API URL (same-origin via nginx proxy)
VITE_API_URL=/api

# ---- Use Hostinger hPanel MySQL (recommended if DB already created) ----
# From your hPanel: database + user = u808821982_Malwa_crm
# Host from Docker on SAME VPS often: host.docker.internal OR mysql hostname from hPanel
# If MySQL is on Hostinger shared (remote), use the host shown in hPanel (e.g. localhost if API on same machine)
DATABASE_URL=mysql+pymysql://u808821982_Malwa_crm:YOUR_DB_PASSWORD@host.docker.internal:3306/u808821982_Malwa_crm?charset=utf8mb4

JWT_SECRET=CHANGE_ME_TO_LONG_RANDOM_SECRET_32CHARS_MIN
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080
CORS_ORIGINS=https://crm.malwatrolley.com,http://localhost

SEED_ADMIN_EMAIL=admin@malwatrolley.com
SEED_ADMIN_PASSWORD=ChangeMe822!
SEED_ADMIN_NAME=Malwa Admin

# ---- Only if using docker-compose.mysql.yml ----
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=malwa_crm
MYSQL_USER=crm_user
MYSQL_PASSWORD=changeme
